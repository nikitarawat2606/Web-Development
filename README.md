# Web-Development

>>Task number 3 submission Done here: CSS & HTML

>>Using inline, internal & external CSS styling in HTML file

# GitHub commands used:
1. Create branch Main
2. git add .
3. git status
4. git commit -m "Message".
5. git push origin main
6. Open a Pull request & merge it with main branch

